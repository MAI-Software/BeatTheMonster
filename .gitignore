node_modules/
dist/
*.apk

# MediaPipe runtime assets — generated at build time (npm run mediapipe / prebuild).
# wasm is copied from node_modules, the model is downloaded once. Keeps them out of git.
public/mediapipe/

# stray tsc output (vite builds from .ts)
src/**/*.js

# capacitor / android build artifacts
android/.gradle/
android/build/
android/app/build/
android/capacitor-cordova-android-plugins/
android/local.properties
.gradle/

# misc
.DS_Store
*.log
.env
.netlify/
